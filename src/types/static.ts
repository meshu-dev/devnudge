export type BlogStaticPathParams = {
  params: {
    slug: string
  }
}

export type BlogListStaticPathParams = {
  params: {
    page: string
  }
}

export type TagStaticPathParams = {
  params: {
    tag: string
    page: string
  }
}
